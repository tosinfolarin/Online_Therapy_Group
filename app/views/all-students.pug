extends layout
block content
    - console.log(data)
    .main-content 
    h1 All stu 
    table(border=1)
        for item in data 
            tr  
                td #{item.id}
                td 
                    a(href='/student-single/' + item.id) #{item.name}